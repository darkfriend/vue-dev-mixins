Vue mixins
=====

Current mixins
==

* isEmpty()
* isArray()

Installation
==

You can install the package via npm:
```shell script
npm i vue-dev-mixins -S
```

Usage
==

```javascript
import Vue from 'vue';
import mixins from 'vue-dev-mixins';

Vue.mixin(mixins);
```